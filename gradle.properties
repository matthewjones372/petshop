# The lark every module builds against. `-PlarkVersion=0.5.1-SNAPSHOT` overrides it, and a snapshot is
# looked for in ~/.m2 too, which is how a lark change is tried here before it is released.
larkVersion=0.7.0
